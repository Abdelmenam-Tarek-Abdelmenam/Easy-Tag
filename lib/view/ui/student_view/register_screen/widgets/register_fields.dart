import 'package:flutter/material.dart';

class RegisterField extends StatelessWidget {
  final List<bool> activeFields;
  const RegisterField(this.activeFields, {Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
